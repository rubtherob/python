'''
Создать вручную список, содержащий цены на товары (10–20 товаров), например:
[57.8, 46.51, 97, ...]

Вывести на экран эти цены через запятую в одну строку, цена должна отображаться в виде <r> руб <kk> коп.
Например:
57 руб 08 коп, 46 руб 51 коп, 97 руб 00 коп, ...

Подумать, как из цены получить рубли и копейки, как добавить нули, если, например, получилось 7 копеек или 0 копеек (должно быть 07 коп или 00 коп).

Вывести цены, отсортированные по возрастанию, новый список не создавать (доказать, что объект списка после сортировки остался тот же).

Создать новый список, содержащий те же цены, но отсортированные по убыванию.

Вывести цены пяти самых дорогих товаров. Сможете ли вывести цены этих товаров по возрастанию, написав минимум кода?

Задачи со * предназначены для продвинутых учеников, которым мало сделать обычное задание.
'''


costs=[57.2, 29.2, 89, 7.81, 124.91, 12.34, 214, 123.67, 29.1]
def print_cost(costs):
    for cost in costs:
        cost_list= str(cost).split('.')
        cost_rub= cost_list[0]
        if len(cost_list) == 2 and int(cost_list[1]) < 10:
            cost_kop ='0' + cost_list[1]
        elif len(cost_list)== 2:
            cost_kop = cost_list[1]
        else:
            cost_kop= '00'
        print("{} рублей {} копеек, ".format(cost_rub,cost_kop),end='')


print_cost(costs)


print('\n' ,id( costs))
costs.sort()
print(costs)
print(id(costs))



costs_new=sorted(costs, reverse=True)
print_cost(costs_new[:5])

five_max_costs=[]

for a in range(5):
    five_max_costs.append( costs.pop (costs.index (max (costs))))

print('\n')
print_cost(five_max_costs)

